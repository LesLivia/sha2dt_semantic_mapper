<template>
<name>DiscoveredSystem</name>
<declaration>
clock x;

/**
** Time CDFs
**/

double Tcdf = 0.0;

**TCDF**

void sample_ecdf(int d) {
	double pr = random(1.0);
	int i;
    bool found = false;

    **SAMPLING_FN**

}


int loc_entity = 0;
int edge_entity = 0;

/**
**DICT**
**/

void update_entities(int loc, int edge) {
    loc_entity = loc;
    edge_entity = edge;
}
</declaration>
**LOCATIONS**<init ref="**INIT_ID**"/>**TRANSITIONS**</template>